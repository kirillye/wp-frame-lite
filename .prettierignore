node_modules/
vendor/

# Сборка Vite
assets/dist/

# Результат npm run images — генерируется, форматировать нечего
assets/webp/

# Сторонние библиотеки: минифицированы, форматировать их нельзя —
# prettier развернул бы их обратно в тысячи строк
assets/vendor/

# ACF перезаписывает эти файлы сам — форматировать их бессмысленно
acf-json/

# Шапка темы, её читает WordPress — не трогаем
style.css

package-lock.json
composer.lock
LICENSE

# PHP форматирует DEVSENSE PHP Tools (см. .vscode/settings.json)
*.php
